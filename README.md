# Run SSH Socks5 Proxy On Container
~~~yaml
    ...
    environment:
      - SSH_HOST=${SSH_HOST}
      - SSH_PORT=${SSH_PORT}
      - SSH_USER=${SSH_USER}
      - SSH_PASSWORD=${SSH_PASSWORD}
      - SSH_KEY=${SSH_KEY}
~~~